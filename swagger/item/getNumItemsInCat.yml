#Gets the number of items in a category
---
tags:
  - getNumItemsInCat
definitions:
  - schema:
      id: GetNumItemsInCat
      properties:
        name:
         type: int
         description: number of items in a category
parameters:
  - in: query
    description: item
    schema:
      type: int
      required: true
#   - in: body
#      #name: category
#        schema:
#          type: integer
#          required: true
responses:
  200:
    description: integer
    schema:
      type: int
      items:
        $ref: '#/definitions/GetNumItemsInCatResponse'
  403:
    description: error
