Adds a new item to the database
---
tags:
  - getItems
definitions:
  - schema:
      id: GetItemsResponse
      properties:
        name:
         type: list
         description: List of all the items
get:
  parameters:
     - in: query
        name: items
          schema:
            type: list
            required: true

responses:
  200:
    description: JWT Object List
    schema:
      type: objectList
      items:
        $ref: '#/definitions/GetItemsResponse'
  403:
    description: error
